rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // L'application utilise Firebase Authentication : aucune lecture/écriture
    // Firestore n'est autorisée sans utilisateur connecté.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
